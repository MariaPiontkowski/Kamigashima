<nav class="navbar">
    <div class="container-fluid">
        <div class="navbar-header">
            <a href="#" onclick="event.preventDefault()" class="bars"></a>
            <a class="navbar-brand" href="{{ route('admin.dashboard') }}">{{ config('app.name') }} - ERP</a>
        </div>
    </div>
</nav>